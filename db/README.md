# db/

Schema and migrations. Ours and PowerDNS's own tables share one database, which is what makes a record write a transaction — see docs/architecture.md.

Nothing here yet. See [`../STATUS.md`](../STATUS.md).
